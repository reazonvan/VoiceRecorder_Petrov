namespace VoiceRecorder_Petrov
{
    // Shell не используется в качестве стартовой страницы (см. App.xaml.cs),
    // но может пригодиться, если позже перейти на Shell-навигацию.
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
        }
    }
}
